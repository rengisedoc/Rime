patch:
  show_notifications_when: appropriate # 状态通知，适当，也可设为全开（always）全关（never）

  style:
    color_scheme: "azure"
    corner_radius: 3
    border_width: 10
    border_height: 0
    font_face: "LXGW WenKai, HanaMinB"
    font_point: 15
    horizontal: true
    inline_preedit: true

  app_options:
    com.github.wez.wezterm: &a
      ascii_mode: true
      no_inline: false
      vim_mode: true
    com.google.Chrome: *a
    com.microsoft.VSCode: *a
    com.DanPristupov.Fork: *a
    com.apple.finder: *a
    com.apple.Spotlight: *a
    com.youdao.YoudaoDict: *a
    com.apple.launchpad.launcher: *a
    com.apple.Terminal: *a
    app.zen-browser.zen: *a
    com.todesktop.230313mzl4w4u92: *a
